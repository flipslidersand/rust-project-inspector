[advisories]
# Add RUSTSEC advisory IDs here only when the risk is understood and accepted.
# Prefer upgrading the dependency; ignore only as a last resort.
# Example: ignore = ["RUSTSEC-2024-0001"]
ignore = []
